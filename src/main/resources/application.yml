spring:
  application:
    name: spring_jpa
  datasource:
#    REALIZANDO CONEC��O COM O BANCO
    url: jdbc:postgresql://localhost:5050/library
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none # ira criar o banco e vai mater os dados
#      ddl-auto: update # ira criar o banco e vai mater os dados
    properties:
      hibernate.format_sql: true # muda a forma como mostra no console a consulta sql